<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="me.ianhe.isite.dao.SysRoleMapper">
    <resultMap id="BaseResultMap" type="me.ianhe.isite.entity.SysRole">
        <id column="id" property="id" jdbcType="INTEGER"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, name
    </sql>

    <select id="getUserRoles" resultMap="BaseResultMap">
        select
        role.*
        from
        sys_role role,sys_role_user ru
        where role.`id` = ru.`roleID` and ru.`userID` = #{userID,jdbcType=INTEGER}
    </select>
    <select id="getAllRoles" resultType="me.ianhe.isite.entity.SysRole">
        select
        <include refid="Base_Column_List"/>
        from
        sys_role
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.ianhe.isite.dao.RoleMapper">
    <select id="roles" resultType="me.ianhe.isite.entity.Role">
        SELECT *
        FROM t_role
        where id != 6;
    </select>
    <insert id="addNewRole">
        INSERT INTO t_role
        set name = #{role}, nameZh = #{roleZh}
    </insert>
    <delete id="deleteRoleById" parameterType="Long">
        DELETE FROM t_role
        WHERE id = #{rid}
    </delete>
</mapper>